name: mdox-cache
description: Cache mdox links for validation
runs:
  using: composite
  steps:
    - name: Cache links for mdox
      uses: actions/cache@v4
      with:
        path: .mdoxcache
        key: mdox-link-cache
