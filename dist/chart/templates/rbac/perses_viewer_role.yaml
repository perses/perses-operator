{{- if .Values.rbac.enable }}
# permissions for end users to view perses.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: perses-viewer-role
rules:
  - apiGroups:
      - perses.dev
    resources:
      - perses
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - perses.dev
    resources:
      - perses/status
    verbs:
      - get
{{- end -}}
