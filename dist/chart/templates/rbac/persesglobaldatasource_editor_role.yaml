{{- if .Values.rbac.enable }}
# permissions for end users to edit persesglobaldatasources.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: persesglobaldatasource-editor-role
rules:
  - apiGroups:
      - perses.dev
    resources:
      - persesglobaldatasources
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch
  - apiGroups:
      - perses.dev
    resources:
      - persesglobaldatasources/status
    verbs:
      - get
{{- end -}}
