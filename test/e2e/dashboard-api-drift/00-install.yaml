apiVersion: perses.dev/v1alpha2
kind: Perses
metadata:
  name: perses-drift-test
  labels:
    app.kubernetes.io/instance: perses-drift-test
spec:
  metadata:
    labels:
      instance: perses-drift-test
  config:
    database:
      file:
        folder: "/perses"
        extension: "yaml"
  containerPort: 8080
  resources:
    requests:
      memory: 128Mi
