apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 60
commands:
  - command: kubectl wait --for=delete persesdashboard/tagged-dashboard -n $NAMESPACE --timeout=60s
  - command: kubectl wait --for=delete perses/perses-tags-test -n $NAMESPACE --timeout=60s
