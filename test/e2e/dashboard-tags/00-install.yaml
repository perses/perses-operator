apiVersion: perses.dev/v1alpha2
kind: Perses
metadata:
  name: perses-tags-test
  labels:
    app.kubernetes.io/instance: perses-tags-test
spec:
  metadata:
    labels:
      instance: perses-tags-test
  config:
    database:
      file:
        folder: "/perses"
        extension: "yaml"
  containerPort: 8080
  resources:
    requests:
      memory: 128Mi
