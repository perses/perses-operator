apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 60
commands:
  - command: kubectl wait --for=delete persesdashboard/promql-valid-dashboard -n $NAMESPACE --timeout=60s
  - command: kubectl wait --for=delete persesdashboard/promql-variable-dashboard -n $NAMESPACE --timeout=60s
