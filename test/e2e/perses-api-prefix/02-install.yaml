apiVersion: perses.dev/v1alpha2
kind: PersesDatasource
metadata:
  name: e2e-api-prefix-datasource
spec:
  instanceSelector:
    matchLabels:
      app.kubernetes.io/instance: perses-api-prefix-test
  config:
    display:
      name: "E2E API Prefix Datasource"
    default: true
    plugin:
      kind: "PrometheusDatasource"
      spec:
        directUrl: "https://prometheus.demo.prometheus.io"
