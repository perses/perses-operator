apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 120
commanapi-prefix:
  - script: kubectl get persesdatasource e2e-api-prefix-datasource -o jsonpath='{.status.conditions[?(@.type=="Available")].reason}' | grep -q Reconciled
