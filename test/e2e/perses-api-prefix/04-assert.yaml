apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 60
commands:
  - command: kubectl wait --for=delete persesdatasource/e2e-api-prefix-datasource --timeout=60s
  - command: kubectl wait --for=delete perses/perses-api-prefix-test -n $NAMESPACE --timeout=60s
