apiVersion: perses.dev/v1alpha2
kind: Perses
metadata:
  name: perses-api-prefix-test
  labels:
    app.kubernetes.io/instance: perses-api-prefix-test
spec:
  metadata:
    labels:
      instance: perses-api-prefix-test
  config:
    api_prefix: /test
    database:
      file:
        folder: "/perses"
        extension: "yaml"
  containerPort: 8080
  resources:
    requests:
      memory: 128Mi
